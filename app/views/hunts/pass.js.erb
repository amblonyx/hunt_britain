var clue = 'pass'
if ($("#popup_confirm").attr("data-current") != clue) {

	$('#popup_confirm h2').text('Give up?')
	$('#popup_confirm p').text('Tried the hint? If you want to give up on this clue, click OK')
	$('#popup_confirm #popup_action').attr("value", 'giveup')

	$("#popup_confirm").attr("data-current", clue)
}

$("#overlay").show()
$("#popup_confirm").show()